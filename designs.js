// Select color input
// Select size input

// When size is submitted by the user, call makeGrid()
row = ('#height');
col = ('#width');
container = ('#pixel_canvas');

$(function makeGrid() {
  for (var n = 0; i < row; y++){
      for (var m = 0; i < col; x++){
        unit.appendTo('container');
    }
  }
})
